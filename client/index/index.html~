<template name="index">
    <h3>
        Tags
	</h>

	<div name="centralize-buttons" style="text-align: center;">
    	<a href="new" class="btn btn-large btn-default btn-home btn-block">Nova</a>
        <hr />
        <table class="table table-striped">
            <tbody>
		        {{#each tags}}
			        {{> tag}}
		        {{/each}}
            </tbody>
        </table>
	</div>
</template>

<template name="tag">
    <tr>
        <td>
            {{{description}}}
        </td>
        <td>
            {{{count}}}
        </td>
        <td style="width: 50px">
	        <a href="#" class="btn btn-large btn-default btn-home btn-block" id="{{_id}}">
                <i class="icon-plus"></i>
            </a>
        </td>
    </tr>
	<br />
	<br />
</template>
