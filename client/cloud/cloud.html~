<template name="cloud">
    <div id="cloud" class="wordcloud">
	    {{#each tags}}
            <span data-weight="{{{count}}}">{{{description}}}</span>
	    {{/each}}
        {{{cloudfy}}}
    </div>
</template>
